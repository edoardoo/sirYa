(function (angular) { 'use strict';

    /**
    * sirya authentication logout module
    */
    angular.module('sirya.ui.authentication.logout', [])
	    .controller(function($scope) {

	    });
})(window.angular);
